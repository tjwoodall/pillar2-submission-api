meta {
  name: Nil return - obligationMTT invalid
  type: http
  seq: 3
}

post {
  url: {{pillar2Url}}/submit-uk-tax-return
  body: json
  auth: none
}

headers {
  X-Pillar2-Id: {{testPlrId}}
  Authorization: {{bearerToken}}
}

body:json {
  {
    "accountingPeriodFrom": "2024-01-01",
    "accountingPeriodTo": "2024-12-31",
    "obligationMTT": true,
    "electionUKGAAP": true,
    "liabilities": {
      "returnType": "NIL_RETURN"
    }
  }
}

tests {
  // Note: Local stub doesn't validate obligationMTT business rules - returns 201
  test("Status code is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
  
  test("Response contains form bundle number", function() {
    expect(res.body.formBundleNumber).to.not.be.undefined;
  });
}
